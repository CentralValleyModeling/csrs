<div class="form-floating my-2">
    <input 
        name={{sect.attr}} 
        class="form-control form-control-sm" 
        placeholder="{{sect.label}}" 
        id="form-item-{{sect.id}}" 
        value="{{sect.default}}" 
        required
    >
    <label for="form-item-{{sect.id}}">{{sect.label}}</label>
</div>