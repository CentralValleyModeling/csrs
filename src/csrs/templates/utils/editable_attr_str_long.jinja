{% extends "utils/_editable_attr.jinja" %}
{% block editable %}
<textarea 
    class="form-control form-control-sm toggled-by-button" 
    id="{{id}}"
    name="{{name}}"
    placeholder="{{default}}" 
    rows="{{rows}}"
    required
    disabled
>{{default}}</textarea>
{% endblock %}