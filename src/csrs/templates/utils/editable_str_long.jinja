<div class="col-1 mx-2"><code>{{name}}</code></div>
<div class="col">
    <textarea 
        class="form-control form-control-sm toggled-by-button" 
        id="{{id}}"
        name="{{name}}"
        placeholder="{{default}}" 
        rows="{{rows}}"
        required
        disabled
    >{{default}}</textarea>
</div>